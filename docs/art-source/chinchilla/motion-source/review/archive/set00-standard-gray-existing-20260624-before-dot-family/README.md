# Chinchilla set00 Existing Archive - 2026-06-24

This is a non-destructive snapshot of the existing `chinchilla_standard_gray` `set00` state before the DeguDesktop-style dot-family trial.

The active files were copied, not moved or deleted. The archive preserves the current 62 accepted frame PNGs, matching prepare reports, set/root audit artifacts, full light/dark contact sheets, the draft sheet, and a SHA256 manifest.

Reason for archive:

- The current set is usable as a prior draft/reference state.
- The next trial intentionally explores a separate, stricter dot-family style.
- The dot-family trial must not overwrite or mix with the existing accepted set until it proves itself in review.
