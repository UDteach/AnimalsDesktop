# Ferret Sable motion source

Status: parent-approved `set00`, accepted locally on 2026-07-26.

- Variant: `ferret_sable`
- Motion profile: `ferret-slink`
- Frames: 62 standalone `96x64` transparent PNGs
- Accepted-frame aggregate SHA-256:
  `33c2da1526d4bd71c7ce76f1e08ca6213406a590b6059d51849514118a4a69f0`
- Source-sheet SHA-256:
  `22322c1fc8a7b98ba79d603192ccc68e36040cea714eac6436f7b5b711482700`
- Full production archive:
  `E:/Development/AnimalDesktop-asset-archives/20260724-ferret-three-variants/ferret-sable-set00-oneframe-62/`

Every accepted frame was produced by its own built-in ImageGen call and copied
byte-for-byte from the parent-approved winner. No grid or sheet cell, local
paint, recolor, warp, interpolation, alpha repair, or tone postprocessing was
promoted. `provenance/manifest.csv` maps every accepted PNG to its exact
prompt, raw/alpha candidate, winning attempt, built-in ImageGen source, and
hashes.

The final action-aware audit reports `valid=62`, `missing=0`, `invalid=0`,
`warnings=0`; the shared matte audit passes `62/62`; and all 62 accepted PNG
hashes are unique. The `contacts/` directory preserves light, checker, and
dark evidence for the full set, rest loop, alert loop, and turn exit.

The assembled source sheet is a deterministic local packaging artifact made
from the accepted standalone frames. It is not generation evidence. Local
acceptance does not authorize publication, a release, tag, or push.
