# Parent promotion - best retry frames 05-09 - 2026-06-27

## Promoted frames

- `frame-05`: retry attempt-02
- `frame-06`: retry attempt-02
- `frame-07`: retry attempt-03
- `frame-08`: retry attempt-02
- `frame-09`: retry attempt-02

Original canonical files were backed up under
`qa/parent-promote-best-retry-05-09-20260627/originals/` before replacement.

## Reason

The selected retries keep the fresh foot-lock lane's green-yellow budgerigar
identity while making the lower-body contact points more visible than the
previous canonical frames. Frame `07` uses attempt-03 because it has the clearest
toe/contact read and slightly more compact scale than attempt-02.

## Parent gate status

The refreshed parent sheet is:

`docs/art-source/one-frame-method-fullrun-20260627/parent-monitor/budgerigar-footlock-after-best-retry-00-11-full-and-footcrop-5x.png`

Parent verdict after this promotion:

- `00-08`: acceptable enough for the fresh foot-lock lane.
- `09-11`: not promotion-ready. Foot/contact points still read as gray smudges,
  and frames `10-11` drift too pale/gray-white around the head/face.

The child thread was instructed to stop broad continuation and produce retry
candidates only for `09`, `10`, and `11`, without overwriting canonical frames.
