# AnimalsDesktop v0.2.11

Release date: 2026-07-02

## Changes

- Removes `true_albino_chipmunk` from the public runtime picker and GitHub Pages current-animal grid.
- Keeps the v0.2.10 beige and ebony chinchilla variants, the blue hooded fancy rat tone fix, and the 12 v0.2.9 Pages candidate animals.
- Documents the albino chipmunk slot as held until a new ImageGen-only source passes review.

## Runtime Scope

- Public selectable animals: 55.
- The previous albino chipmunk source files remain as comparison/source evidence only.
- The next repair lane should generate a fresh ImageGen-only albino chipmunk candidate before this slot returns to runtime.

## Validation

- `go run ./cmd/importanimals`
- `go run ./cmd/validatemotion -runtime-only -require-accepted`
- `python3 scripts/build_page_assets.py`
- `python3 scripts/verify_page_release.py`
- `go test -buildvcs=false ./...`
- `go vet -buildvcs=false ./...`
- macOS arm64/amd64 release builds
- `git diff --check`
